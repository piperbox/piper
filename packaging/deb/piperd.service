[Unit]
Description=Piper agent (git push -> live HTTPS on your own box)
After=docker.service network-online.target
Wants=docker.service network-online.target

[Service]
Type=simple
ExecStart=/usr/bin/piperd
Environment=PIPER_DATA_DIR=/var/lib/piper
Environment=XDG_DATA_HOME=/var/lib/piper
Environment=XDG_CONFIG_HOME=/var/lib/piper
EnvironmentFile=-/etc/piper/piperd.env
DynamicUser=yes
SupplementaryGroups=docker
StateDirectory=piper
StateDirectoryMode=0700
AmbientCapabilities=CAP_NET_BIND_SERVICE
CapabilityBoundingSet=CAP_NET_BIND_SERVICE
NoNewPrivileges=yes
ProtectSystem=strict
ProtectHome=yes
PrivateTmp=yes
Restart=on-failure
RestartSec=2s

[Install]
WantedBy=multi-user.target
